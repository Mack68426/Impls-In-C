#include <stdio.h>
#include <stdlib.h>
#include <string.h>
// #include <windows.h>
#include "basis.h"
#include "byte.h"

int main()
{
/*     
    Array* arr = malloc(sizeof(Array));

    int d[] = {1, 10, 32, 102};

    arr->data = d;

    for (int i = 0; i < 4; i++)
    {
        printf("%d%s", arr->data[i], i<4?" ":"\n");
    }
 */
    test();

    
    return 0;
}